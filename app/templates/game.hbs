{{this.userInput}}
{{#each (alphabet) as |char|}}
  <button
    type="button"
    {{on 'click' (fn this.handleInput char)}}
    {{on-key (concat 'Key' char)}}
  >
    {{char}}
  </button>
{{/each}}
<button {{on 'click' this.handleSubmit}}>
  submit
</button>